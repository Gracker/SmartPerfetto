<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->
<!-- "requirements": active arch-conditional calls. Non-droppable — keep short. -->
## 计划强制项（当前架构）

`submit_plan` 的 `expectedCalls` 必须含：

{{requirements}}

未列出的架构分支不要写进计划；执行时信号缺失用 `skipped` + 原因。
